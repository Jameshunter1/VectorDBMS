#include <core_engine/storage/page.hpp>

namespace core_engine {
// Page is currently a trivial type; implementation file exists to stabilize
// the build structure as the class grows (checksums, LSNs, pin counts, etc.).
}
